terraform {
  required_providers {
    loader = {
      source  = "aicl/file_loader"
      version = "~> 1.0"
      container = {
        image = "aicl/file_loader:1.0.0"
      }
    }
  }
}

resource "loader_files" "docs_loader" {
  path = "./docs"
  glob = "**/*.md"
}
