# AICL Improved CLI Dependencies
#
# Install with: pip install -r requirements-cli.txt

# CLI Framework (better than argparse)
typer[all]>=0.9.0

# Configuration Management (Python's Viper)
dynaconf>=3.2.0

# Beautiful Terminal Output (unique to Python)
rich>=13.0.0

# Amazing Logging (simpler than stdlib logging)
loguru>=0.7.0

# Type-Safe Config Models
pydantic>=2.0.0

# Optional but recommended
python-dotenv>=1.0.0  # Already in main deps
pyyaml>=6.0.0         # Already in main deps
