terraform {
  required_providers {
    openrouter = {
      source = "aicl/openrouter"
    }
  }
}

resource "chat" "test_response" {
  model = "{{ model }}"
  messages = [
    {
      role = "user"
      content = "{{ task }}: {{ topic }}"
    }
  ]
  aiclResourceName = "test_response"
}
