terraform {
  required_providers {
    naga = {
      source = "aicl/naga"
    }
  }
}

# Test Naga embedding
resource "naga_embedding" "test" {
  model = "text-embedding-3-small"
  text = "Hello world"
  aiclResourceName = "test"
}
